
const multiplyNumeric = (obj,num) => {
    
    {
        for (let key in obj) 
        {
            let value = obj[key];
        if (typeof value  === 'number'){
            object[key] = object[key] *num
        }
        
       
    }
}
}

let menu = 
{
    width: 200,
    height: 300,
    title: 'My menu'
};